const express = require('express');

const authenticated = express.Router();

// Minimal authenticated router stub (additional auth routes can be added here)

module.exports.authenticated = authenticated;

